<!DOCTYPE html>
<html>
<body>
    <ul>
    {{#each people}}
        <li>[{{id}}]: {{first_name}} {{last_name}} / {{date_of_birth}} / {{gender}} / {{email}} / {{job_title}} </li>
    {{/each}}
    </ul>
</body>
</html>
